1.0版本
//收藏夹功能，点击重置时。输入框自动获取焦点。
//收藏夹输入框的设置弹出窗口，的例如根下面有点重复。把例如做成一个小的tips按钮
//启动时的欢迎语显示域名访问地址。
//增加配置：域名
输入框获取焦点是，支持方向键定位书签。上下左右
首页的收藏夹输入框获取焦点时，支持方向键定位书签。上下
首页的IP地址，显示当前登录IP

1.1版本
增加配置：备案，左下角显示备案信息。及是否显示备案信息。及显示位置。
增加个人中心功能：修改个人资料。
solrController.js的增删改查代码需要优化为封装调用
增加配置中心，主要配置是对config文件更新。实现后期服务扩展的自定义维护。
用户登录，登录持久化。

增加日历功能。查看节假日，阴历阳历信息。假节日名称，节气。

服务器监控
查看服务器内存数据

仓库管理系统

考勤管理系统

浏览器历史记录分析， 分析domain占比 饼图


//! 检测是否有static，config文件夹 打包后需要检测是否有node_sqlite3.node文件
//! 如何判断是否是打包状态呢？？？
//!通过let sqliteDbPath = process.cwd()+"/db/base.db"; 方式查找 node_modules，package.json 文件。
//!如果找不到那就是打包环境